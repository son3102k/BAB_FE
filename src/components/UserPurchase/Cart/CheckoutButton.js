import React from "react";
import Button from "@mui/material/Button";

export default function defaultButton() {
        return (
            <Button>
                Checkout
            </Button>
        );
}
